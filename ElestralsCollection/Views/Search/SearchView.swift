import SwiftUI

struct SearchView: View {
    var body: some View {
        NavigationStack {
            ZStack {
                Color.green
            }
            .navigationTitle("Search")
        }
    }
}
