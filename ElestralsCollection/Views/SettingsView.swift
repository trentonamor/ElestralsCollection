import SwiftUI

struct SettingsView: View {
    var body: some View {
        NavigationView {
            ZStack {
                Image("001.png")
            }
            .navigationTitle("Settings")
        }
    }
}
