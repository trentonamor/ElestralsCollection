import SwiftUI

struct ExpansionsView: View {
    var body: some View {
        NavigationView {
            ZStack {
                Color.blue
            }
            .navigationTitle("Expansions")
        }
    }
}

