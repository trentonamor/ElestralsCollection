import SwiftUI

struct BookmarkView: View {
    var body: some View {
        NavigationStack {
            ZStack {
                Color.purple
            }
            .navigationTitle("Bookmarks")
        }
    }
}
