import SwiftUI

struct BookmarkView: View {
    var body: some View {
        NavigationView {
            ZStack {
                Color.purple
            }
            .navigationTitle("Bookmarks")
        }
    }
}
